[project]
name = "pr-reviewer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
    "anyio",
]

[project.scripts]
pr-reviewer = "src.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
